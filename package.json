{
  "name": "eslint-config-pob",
  "version": "29.2.0",
  "private": true,
  "author": "Christophe Hurpeau <christophe@hurpeau.com> (http://christophe.hurpeau.com/)",
  "license": "ISC",
  "repository": "https://github.com:christophehurpeau/eslint-config-pob.git",
  "homepage": "https://github.com/christophehurpeau/eslint-config-pob",
  "engines": {
    "node": ">=6.5.0"
  },
  "workspaces": ["@pob/*"],
  "scripts": {
    "lint": "lerna run --stream lint",
    "preversion": "yarn run lint --since && repository-check-dirty",
    "release": "GH_TOKEN=$POB_GITHUB_TOKEN lerna version --conventional-commits --github-release -m 'chore: release' && lerna publish from-git"
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -e $GIT_PARAMS",
      "post-checkout": "yarnhook",
      "post-merge": "yarnhook",
      "post-rewrite": "yarnhook",
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "yarn.lock": ["yarn-update-lock", "git add"],
    "{package.json,@pob/*/package.json,.eslintrc.json}": [
      "prettier --parser json --write",
      "git add"
    ],
    "{@pob/*}/lib/**/*.json": ["prettier --parser json --write", "git add"],
    "{@pob/*}/lib/**/*.js": ["eslint --fix --quiet", "git add"]
  },
  "prettier": {
    "trailingComma": "es5",
    "singleQuote": true,
    "arrowParens": "always"
  },
  "commitlint": {
    "extends": ["@commitlint/config-conventional"]
  },
  "devDependencies": {
    "@commitlint/cli": "7.5.2",
    "@commitlint/config-conventional": "7.5.0",
    "eslint": "5.16.0",
    "husky": "1.3.1",
    "lerna": "3.13.3",
    "lint-staged": "8.1.5",
    "prettier": "1.17.0",
    "repository-check-dirty": "1.0.2",
    "yarn-update-lock": "0.5.0",
    "yarnhook": "0.4.0"
  }
}
